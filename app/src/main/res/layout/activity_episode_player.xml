<?xml version="1.0" encoding="utf-8"?>

    <!-- ============================================================
         SUPERPODCAST EPISODE PLAYER

         Media3 audio player screen.

         Displays:
         1. Episode title
         2. Media3 playback controls
         3. Close button

         Media3 PlayerView provides:
         - Play / Pause
         - Seek forward / backward
         - Playback progress
         - Audio controls
         ============================================================ -->

<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="20dp"
    android:background="#FFFFFF">

    <!-- ========================================================
         PLAYER SCREEN TITLE
         ======================================================== -->

    <TextView
        android:id="@+id/textViewPlayerScreenTitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Now Playing"
        android:textColor="#1F2937"
        android:textSize="22sp"
        android:textStyle="bold"
        android:gravity="center"
        android:layout_marginBottom="16dp" />

    <!-- ========================================================
         EPISODE TITLE
         ======================================================== -->

    <TextView
        android:id="@+id/textViewPlayerEpisodeTitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Podcast Episode"
        android:textColor="#374151"
        android:textSize="18sp"
        android:textStyle="bold"
        android:gravity="center"
        android:maxLines="3"
        android:ellipsize="end"
        android:layout_marginBottom="20dp" />

    <!-- ========================================================
         MEDIA3 PLAYER VIEW
         ========================================================

         Media3 provides the playback controls automatically.

         Controls include:
         - Play
         - Pause
         - Seek
         - Progress
         ======================================================== -->

    <androidx.media3.ui.PlayerView
        android:id="@+id/playerView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="10dp"
        android:layout_marginBottom="20dp"
        app:show_buffering="when_playing"
        app:show_shuffle_button="false"
        app:show_subtitle_button="false"
        app:show_previous_button="false"
        app:show_next_button="false" />

    <!-- ========================================================
         CLOSE PLAYER BUTTON
         ======================================================== -->

    <Button
        android:id="@+id/buttonClosePlayer"
        android:layout_width="wrap_content"
        android:layout_height="48dp"
        android:layout_gravity="center"
        android:text="Close Player"
        android:textSize="14sp"
        android:textAllCaps="false" />

</LinearLayout>
